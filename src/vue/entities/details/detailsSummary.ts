export type DetailsSummaryProps = {
    /** any plain object; collections and nested objects render recursively (values flow through `any` so the recursive binding type-checks) */
    modelValue?: Record<string, any>
}
